## Standard header
## HTML Comments should be commented out when not debugging
## <!-- begin footer -->
#if( ${hide-footer} != "true" && ${hide-footer} != "True")
<div id="footer">
    <div class="left"><p>${footer-left-text}</p></div>
    <div class="content"></div>
    <div class="right">${footer-right-text}${version}</div>
</div>
#end