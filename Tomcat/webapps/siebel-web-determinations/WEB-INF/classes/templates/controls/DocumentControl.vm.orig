#if (${control.isVisible()})
    #parse( "investigation/controlMessages.vm" )
    #parse( "investigation/input-style-overrides.vm" )
    <div id="${control.getEncodedID()}" ${classAttribute} ${styleAttribute}>#parse ( "investigation/Link.vm" )</div>
#end